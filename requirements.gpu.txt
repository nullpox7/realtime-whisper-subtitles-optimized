# Real-time Whisper Subtitles - GPU Optimized Dependencies (v2.2.3)
# CUDA 12.4+ cuDNN optimized for large-v3 model maximum accuracy
# Encoding: UTF-8

# Web Framework (essential)
fastapi==0.111.0
uvicorn[standard]==0.30.1
websockets==12.0
jinja2==3.1.4
python-multipart==0.0.9

# PyTorch with CUDA 12.4 support (installed separately in Dockerfile)
# torch==2.5.1+cu124 --index-url https://download.pytorch.org/whl/cu124
# torchaudio==2.5.1+cu124 --index-url https://download.pytorch.org/whl/cu124

# Core AI/ML (GPU optimized versions)
faster-whisper==1.0.3
transformers==4.42.3
accelerate==0.32.1

# Audio Processing (high-quality, compatible)
librosa==0.10.2
soundfile==0.12.1

# Scientific Computing - GPU optimized and stable versions
numpy>=1.24.0,<2.0.0
scipy==1.13.1

# CUDA optimization libraries for CUDA 12.4
cupy-cuda12x==13.2.0

# Redis client for caching and performance
redis==5.0.7
aioredis==2.0.1

# System utilities
psutil==6.0.0
python-dotenv==1.0.1
requests==2.32.3
aiofiles==24.1.0

# Production server
gunicorn==22.0.0

# Monitoring and metrics
prometheus-client==0.20.0

# Essential utilities
tqdm==4.66.4
packaging==24.1

# Async HTTP client
httpx==0.27.0

# Logging utilities
structlog==24.2.0

# Date/time handling
python-dateutil==2.9.0

# Configuration management
pyyaml==6.0.1

# Memory optimization
pympler==0.9

# Additional ML/Audio dependencies optimized for GPU
resampy==0.4.3
audioread==3.0.1
pooch>=1.8.2
joblib>=1.4.2
threadpoolctl>=3.2.0

# Audio feature extraction (latest compatible versions)
pyworld==0.3.4
pysptk==1.0.1

# Advanced audio processing (stable version)
noisereduce==3.0.2

# Additional performance libraries
orjson==3.10.6
msgpack==1.0.8

# Development and debugging (optional)
ipython==8.26.0
jupyter==1.0.0

# Model downloading and caching
huggingface-hub==0.24.2

# CUDA 12.4 specific optimizations (compatible versions)
nvidia-cudnn-cu12==9.1.0.70
nvidia-cufft-cu12==11.2.1.3
nvidia-curand-cu12==10.3.5.147
nvidia-cusolver-cu12==11.6.1.9
nvidia-cusparse-cu12==12.3.1.170
nvidia-nccl-cu12==2.20.5

# Triton for GPU kernel optimization (compatible version)
triton==2.3.1

# Additional CUDA utilities
pynvml==11.5.0