# Real-time Whisper Subtitles - GPU Optimized Dependencies (v2.2.3)
# CUDA 11.8 + PyTorch 2.1.2 ultra-stable configuration - All symbol errors resolved
# Encoding: UTF-8

# Web Framework (essential)
fastapi==0.111.0
uvicorn[standard]==0.30.1
websockets==12.0
jinja2==3.1.4
python-multipart==0.0.9

# PyTorch with CUDA 11.8 support - ULTRA STABLE VERSION
# These will be installed separately in Dockerfile to ensure compatibility
# torch==2.1.2+cu118 --index-url https://download.pytorch.org/whl/cu118
# torchaudio==2.1.2+cu118 --index-url https://download.pytorch.org/whl/cu118

# Core AI/ML (compatible versions with PyTorch 2.1.2)
faster-whisper==1.0.3
transformers==4.35.2
accelerate==0.24.1

# Audio Processing (high-quality, stable versions)
librosa==0.10.1
soundfile==0.12.1

# Scientific Computing - stable versions compatible with PyTorch 2.1.2
numpy>=1.24.0,<1.26.0
scipy==1.11.4

# Redis client for caching and performance
redis==5.0.7
aioredis==2.0.1

# System utilities
psutil==6.0.0
python-dotenv==1.0.1
requests==2.31.0
aiofiles==23.2.1

# Production server
gunicorn==21.2.0

# Monitoring and metrics
prometheus-client==0.19.0

# Essential utilities
tqdm==4.66.1
packaging==23.2

# Async HTTP client
httpx==0.25.2

# Logging utilities
structlog==23.2.0

# Date/time handling
python-dateutil==2.8.2

# Configuration management
pyyaml==6.0.1

# Memory optimization
pympler==0.9

# Additional ML/Audio dependencies compatible with PyTorch 2.1.2
resampy==0.4.2
audioread==3.0.1
pooch>=1.8.0
joblib>=1.3.2
threadpoolctl>=3.2.0

# Audio feature extraction (stable versions)
pyworld==0.3.2

# Additional performance libraries
orjson==3.9.10
msgpack==1.0.7

# Model downloading and caching
huggingface-hub==0.19.4

# CUDA 11.8 specific libraries (compatible with PyTorch 2.1.2+cu118)
nvidia-cudnn-cu11>=8.7.0
nvidia-cufft-cu11>=10.9.0
nvidia-curand-cu11>=10.3.0
nvidia-cusolver-cu11>=11.4.0
nvidia-cusparse-cu11>=11.7.0
nvidia-nccl-cu11>=2.16.0

# Additional CUDA utilities
pynvml==11.5.0

# Remove ALL problematic dependencies that cause symbol conflicts
# Removed: cupy-cuda11x, cupy-cuda12x - causes symbol conflicts
# Removed: triton - version incompatibility with stable PyTorch
# Removed: pysptk - compilation issues
# Removed: noisereduce - dependency conflicts  
# Removed: ipython/jupyter - to minimize dependencies
# Removed: advanced audio processing libraries - potential conflicts

# Keep only essential, well-tested dependencies for maximum stability
