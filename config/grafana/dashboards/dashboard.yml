# Grafana Dashboard Provisioning Configuration
# Encoding: UTF-8

apiVersion: 1

providers:
  - name: 'Whisper Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
